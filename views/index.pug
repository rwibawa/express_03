extends layout

block content
  h1= title
  p Welcome to #{title}

  div(ng-repeat='product in store.products')
    h2 Name: {{product.name}}
    h3 Price: {{product.price | currency}}
    p Description: {{product.description}}

  <!--[if lte IE 8]> <script src="vendor/angular-ui/build/angular-ui-ieshiv.js"></script><![endif]-->
